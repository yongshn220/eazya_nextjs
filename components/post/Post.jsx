import {PostType} from "@components/post/constants";


export default function Post({type, post}) {

  return (
    <section>
      post
      <div>
        add comment area
      </div>
      <div>
        comment and reply area
      </div>
    </section>

  )
}
